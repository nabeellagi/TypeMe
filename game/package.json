{
	"name": "game",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"gsap": "^3.14.2",
		"kaplay": "^3001.0.19",
		"rxjs": "^7.8.2"
	},
	"devDependencies": {
		"@types/node": "^25.0.3",
		"vite": "^7.3.0"
	}
}
